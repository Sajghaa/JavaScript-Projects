// AlertModal.js - Manages alert modal
class AlertModal {
    constructor(stateManager, eventBus) {
        this.stateManager = stateManager;
        this.eventBus = eventBus;
    }
}

window.AlertModal = AlertModal;